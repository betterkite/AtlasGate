# 功能名称

> ID: `XXX-001`  
> 状态: `implemented | partial | fallback | planned | deprecated`  
> 读者: 开发者 / 运维 / 前端开发者  
> 最后验证: YYYY-MM-DD

## 1. 目的与边界

说明解决的问题、调用方、用户价值和明确不负责的事情。

## 2. 功能契约

说明输入、输出、HTTP 或内部接口、成功条件、稳定错误码和权限要求。

## 3. 代码地图

| 层级 | 文件 | 符号或入口 | 职责 |
|---|---|---|---|
| 入口 | `src/app.js` | `METHOD /path` | 接收请求 |
| 服务 | `src/services/example.js` | `ExampleService.method()` | 业务规则 |
| 核心 | `src/core/example.js` | `function()` | 算法或转换 |
| 存储 | `src/db.js` | `table_name` | 数据持久化 |
| 测试 | `test/example.test.js` | `describe(...)` | 行为证据 |

## 4. 执行流程

用时序图、流程图或伪代码描述正常路径。

## 5. 实现原理

解释关键算法、事务、状态机、缓存、队列、并发和版本语义。

## 6. 数据模型与生命周期

说明表、关键字段、索引、创建、更新、发布、删除和审计保留策略。

## 7. 不变量与安全边界

列出必须保持的业务规则，以及密钥、用户输入、文件路径和外部服务边界。

## 8. 失败、重试与降级

说明超时、重复、并发冲突、外部服务不可用、进程崩溃时的行为。

## 9. 测试与验证

列出测试文件、命令、关键场景和尚未覆盖的风险。

## 10. 扩展方式

说明新增 Provider、协议、页面类型、检索后端或前端视图需要修改的边界。

## 11. 当前限制

不要把 mock、fallback 或预留接口写成完整生产能力。

## 12. 相关文档

